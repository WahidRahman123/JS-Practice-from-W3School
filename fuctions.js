function anyName(name){
  console.log(`${name}!!!! Hello World`);
}

//Calling a function,
anyName('Wahid Rahman')


//* In JavaScript, 'function calling' is called as '"Invoked"'.
/*
Function Invokation:
--------------------
The code inside the functino will execute when "something" invokes (calls) the  function:

1. When an event occurs (when a user clicks a button)
2. When it is invoked (called) from JavaScript code
3. Automatically (self invoked)
*/

